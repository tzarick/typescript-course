// enum - enumeration: purpose of an enum is signaling to other people. Collection of very closely related vals
// enums also declare a new type
export enum MatchResult {
  HomeWin = 'H',
  AwayWin = 'A',
  Draw = 'D',
}
